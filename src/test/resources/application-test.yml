spring:
  datasource:
    url: jdbc:mysql://localhost:3306/ggoggodb_test?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: ggoggo
    password: ggoggo
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop   # 테스트용 권장(시작시 생성, 종료시 삭제)
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: never
